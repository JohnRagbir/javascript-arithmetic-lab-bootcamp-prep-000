function inc (a) {
  return ++a
}

function dec (a) {
  return --a
}

add = (a, b) => a + b; 
  


subtract = (a, b) => {a - b; }

number -= 2;
  

divide = (a, b) =>

{ return a / b; 
  
}

multiply = (a, b) =>

{ return a * b; 
  
}


function inc(n){
  return n += 1;
}

function dec(n){
  return n -= 1;
}

function makeInt(n){
  return parseInt(n, 10);
}

function preserveDecimal(n){
  return parseFloat(n);
}
