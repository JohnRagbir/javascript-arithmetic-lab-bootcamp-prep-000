function inc (a) {
  return ++a
}

function dec (a) {
  return --a
}

add = (a, b) => a + b; 
  


subtract = (a, b) => {a - b; }
  

divide = (a, b) =>

{ return a / b; 
  
}

multiply = (a, b) =>

{ return a * b; 
  
}
