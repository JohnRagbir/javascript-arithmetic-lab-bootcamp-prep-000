function inc (a) {
  return ++a
}

function dec (a) {
  return --a
}

add = (a, b) => 

{ return a + b; 
  
}

subtract = (a, b) =>

{ return b - a; 
  
}

divide = (a, b) =>

{ return a /= b; 
  
}

multiply = (a, b) =>

{ return a * b; 
  
}

Number.parseInt('100', 10);